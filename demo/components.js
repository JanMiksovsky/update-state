// This file contains the simple "components" used in the demo. These are just
// functions that return HTML strings.

// Message component: renders the count as a sentence.
export const message = (count) => `
  The counter is currently <strong>${count}</strong>.
`;
